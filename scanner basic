-- [[ OMEGA NEON SCANNER v8.0 // FULL FIX ]] --
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

-- 1. CLEANUP PREVIOUS VERSIONS
if PlayerGui:FindFirstChild("NeonDeepScanner_FinalFix") then
    PlayerGui.NeonDeepScanner_FinalFix:Destroy()
end

-- 2. MAIN GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NeonDeepScanner_FinalFix"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- 3. THE DRAGGABLE SYSTEM (FULLY TESTED)
local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- 4. MAIN CONTAINER
local Main = Instance.new("Frame")
Main.Name = "MainFrame"
Main.Size = UDim2.new(0, 420, 0, 360)
Main.Position = UDim2.new(0.5, -210, 0.5, -180)
Main.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = false -- We use our custom script instead
Main.Parent = ScreenGui
makeDraggable(Main)

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)
local MainStroke = Instance.new("UIStroke", Main)
MainStroke.Color = Color3.fromRGB(57, 255, 20)
MainStroke.Thickness = 2

-- 5. TOP BAR (TITLE, MIN, CLOSE)
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -100, 0, 40)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Text = "SYSTEM_OMEGA // FIXED"
Title.TextColor3 = Color3.fromRGB(57, 255, 20)
Title.Font = Enum.Font.Code
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -40, 0, 5)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.Parent = Main
Instance.new("UICorner", CloseBtn)

local MiniBtn = Instance.new("TextButton")
MiniBtn.Size = UDim2.new(0, 30, 0, 30)
MiniBtn.Position = UDim2.new(1, -75, 0, 5)
MiniBtn.Text = "-"
MiniBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MiniBtn.TextColor3 = Color3.fromRGB(57, 255, 20)
MiniBtn.Font = Enum.Font.SourceSansBold
MiniBtn.Parent = Main
Instance.new("UICorner", MiniBtn)

-- 6. SIDE PANEL (THE EXECUTION INTERFACE)
local Side = Instance.new("Frame")
Side.Name = "SidePanel"
Side.Size = UDim2.new(0, 220, 1, 0)
Side.Position = UDim2.new(1, 15, 0, 0)
Side.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Side.Visible = false
Side.Parent = Main
Instance.new("UICorner", Side)
Instance.new("UIStroke", Side).Color = Color3.fromRGB(57, 255, 20)

local CommandInput = Instance.new("TextBox")
CommandInput.Size = UDim2.new(0.9, 0, 0, 45)
CommandInput.Position = UDim2.new(0.05, 0, 0.15, 0)
CommandInput.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
CommandInput.TextColor3 = Color3.fromRGB(255, 255, 255)
CommandInput.Font = Enum.Font.Code
CommandInput.ClearTextOnFocus = false
CommandInput.TextSize = 14
CommandInput.Parent = Side
Instance.new("UICorner", CommandInput)

local ExecuteBtn = Instance.new("TextButton")
ExecuteBtn.Size = UDim2.new(0.9, 0, 0, 40)
ExecuteBtn.Position = UDim2.new(0.05, 0, 0.32, 0)
ExecuteBtn.BackgroundColor3 = Color3.fromRGB(57, 255, 20)
ExecuteBtn.Text = "EXECUTE"
ExecuteBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
ExecuteBtn.Font = Enum.Font.SourceSansBold
ExecuteBtn.Parent = Side
Instance.new("UICorner", ExecuteBtn)

-- 7. SEARCH & LIST AREA
local Search = Instance.new("TextBox")
Search.Size = UDim2.new(1, -20, 0, 35)
Search.Position = UDim2.new(0, 10, 0, 45)
Search.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Search.PlaceholderText = "Search scanned data..."
Search.TextColor3 = Color3.fromRGB(255, 255, 255)
Search.Parent = Main
Instance.new("UICorner", Search)

local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(1, -20, 1, -100)
Scroll.Position = UDim2.new(0, 10, 0, 95)
Scroll.BackgroundTransparency = 1
Scroll.ScrollBarThickness = 4
Scroll.Parent = Main
Instance.new("UIListLayout", Scroll).Padding = UDim.new(0, 5)

-- 8. MINIMIZE LOGIC
local minimized = false
local originalSize = Main.Size
MiniBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    for _, v in pairs(Main:GetChildren()) do
        if v ~= Title and v ~= MiniBtn and v ~= CloseBtn and v ~= MainStroke and not v:IsA("UICorner") then
            v.Visible = not minimized
        end
    end
    Main.Size = minimized and UDim2.new(0, 420, 0, 40) or originalSize
end)

CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

-- 9. CORE FUNCTIONALITY
local clickConn = nil
local function addEntry(obj, typeTag)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(1, 0, 0, 30)
    b.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    b.Text = " [" .. typeTag .. "] " .. obj.Name
    b.TextColor3 = Color3.fromRGB(57, 255, 20)
    b.TextXAlignment = Enum.TextXAlignment.Left
    b.Parent = Scroll
    Instance.new("UICorner", b)

    b.MouseButton1Click:Connect(function()
        Side.Visible = true
        CommandInput.Text = obj.Name
        
        if clickConn then clickConn:Disconnect() end
        clickConn = ExecuteBtn.MouseButton1Click:Connect(function()
            if typeTag == "GUI" then obj.Enabled = not obj.Enabled end
            ExecuteBtn.Text = "SUCCESS"
            task.wait(0.5)
            ExecuteBtn.Text = "EXECUTE"
        end)
    end)
end

local function runScan()
    for _, v in pairs(Scroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    for _, g in pairs(PlayerGui:GetChildren()) do
        if g:IsA("ScreenGui") and g ~= ScreenGui then addEntry(g, "GUI") end
    end
    local function scan(parent)
        for _, child in pairs(parent:GetChildren()) do
            if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
                addEntry(child, "CMD")
            elseif not child:IsA("Player") then
                pcall(function() scan(child) end)
            end
        end
    end
    scan(ReplicatedStorage)
    scan(workspace)
end

Search:GetPropertyChangedSignal("Text"):Connect(function()
    local f = Search.Text:lower()
    for _, b in pairs(Scroll:GetChildren()) do
        if b:IsA("TextButton") then b.Visible = b.Text:lower():find(f) ~= nil end
    end
end)

task.spawn(function()
    while true do
        runScan()
        task.wait(2)
    end
end)
