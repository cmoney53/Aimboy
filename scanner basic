-- [[ OMEGA NEON OMNI-SCANNER v13.0 // INFINITE SCROLL & GOD-SCAN ]] --
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

-- 1. CLEANUP
if PlayerGui:FindFirstChild("OmegaOmniFinal_V13") then 
    PlayerGui.OmegaOmniFinal_V13:Destroy() 
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "OmegaOmniFinal_V13"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- 2. STABLE DRAGGABLE SYSTEM
local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true; dragStart = input.Position; startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- 3. MAIN UI WINDOW
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 460, 0, 420)
Main.Position = UDim2.new(0.5, -230, 0.5, -210)
Main.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
Main.BorderSizePixel = 0
Main.Active = true
Main.Parent = ScreenGui
makeDraggable(Main)

Instance.new("UICorner", Main)
local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = Color3.fromRGB(57, 255, 20)
Stroke.Thickness = 2

-- 4. TITLE & CONTROLS
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -100, 0, 40)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Text = "SYSTEM_OMEGA // GOD_VIEW V13"
Title.TextColor3 = Color3.fromRGB(57, 255, 20)
Title.Font = Enum.Font.Code
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Parent = Main
Instance.new("UICorner", CloseBtn)
CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

-- 5. SIDE PANEL (DATA EDIT & EXECUTE)
local Side = Instance.new("Frame")
Side.Size = UDim2.new(0, 240, 1, 0)
Side.Position = UDim2.new(1, 15, 0, 0)
Side.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Side.Visible = false
Side.Parent = Main
Instance.new("UICorner", Side)
Instance.new("UIStroke", Side).Color = Color3.fromRGB(57, 255, 20)

local CommandBox = Instance.new("TextBox")
CommandBox.Size = UDim2.new(0.9, 0, 0, 45)
CommandBox.Position = UDim2.new(0.05, 0, 0.1, 0)
CommandBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
CommandBox.TextColor3 = Color3.fromRGB(255, 255, 255)
CommandBox.ClearTextOnFocus = false
CommandBox.Font = Enum.Font.Code
CommandBox.Parent = Side
Instance.new("UICorner", CommandBox)

local ExecuteBtn = Instance.new("TextButton")
ExecuteBtn.Size = UDim2.new(0.9, 0, 0, 40)
ExecuteBtn.Position = UDim2.new(0.05, 0, 0.25, 0)
ExecuteBtn.BackgroundColor3 = Color3.fromRGB(57, 255, 20)
ExecuteBtn.Text = "EXECUTE / SET"
ExecuteBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
ExecuteBtn.Font = Enum.Font.SourceSansBold
ExecuteBtn.Parent = Side
Instance.new("UICorner", ExecuteBtn)

local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(0.9, 0, 0, 220)
InfoLabel.Position = UDim2.new(0.05, 0, 0.4, 0)
InfoLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
InfoLabel.BackgroundTransparency = 1
InfoLabel.TextWrapped = true
InfoLabel.TextYAlignment = Enum.TextYAlignment.Top
InfoLabel.Font = Enum.Font.SourceSans
InfoLabel.Parent = Side

-- 6. SCROLLING LIST (INFINITE FIX)
local Search = Instance.new("TextBox")
Search.Size = UDim2.new(1, -30, 0, 35)
Search.Position = UDim2.new(0, 15, 0, 45)
Search.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Search.PlaceholderText = "Search Commands, Stats, GUIs..."
Search.TextColor3 = Color3.fromRGB(255, 255, 255)
Search.Parent = Main
Instance.new("UICorner", Search)

local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(1, -30, 1, -110)
Scroll.Position = UDim2.new(0, 15, 0, 95)
Scroll.BackgroundTransparency = 1
Scroll.ScrollBarThickness = 6
Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
Scroll.Parent = Main

local ListLayout = Instance.new("UIListLayout", Scroll)
ListLayout.Padding = UDim.new(0, 5)

-- THE FIX: Dynamic Canvas Resize for Infinite Scrolling
ListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    Scroll.CanvasSize = UDim2.new(0, 0, 0, ListLayout.AbsoluteContentSize.Y + 20)
end)

-- 7. THE SCAN ENGINE (GOD-MODE)
local clickConn = nil
local function addEntry(obj, typeTag)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(1, 0, 0, 32)
    b.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    b.Text = " [" .. typeTag .. "] " .. obj.Name
    
    -- Visual Coding
    if typeTag:find("GUI") then b.TextColor3 = Color3.fromRGB(0, 200, 255)
    elseif typeTag == "DATA" then b.TextColor3 = Color3.fromRGB(255, 180, 0)
    elseif typeTag == "CMD" then b.TextColor3 = Color3.fromRGB(57, 255, 20)
    else b.TextColor3 = Color3.fromRGB(200, 200, 200) end
    
    b.TextXAlignment = Enum.TextXAlignment.Left
    b.Parent = Scroll
    Instance.new("UICorner", b)

    -- Persistent Search Logic
    if not b.Text:lower():find(Search.Text:lower()) then b.Visible = false end

    b.MouseButton1Click:Connect(function()
        Side.Visible = true
        if typeTag == "DATA" then
            CommandBox.Text = tostring(obj.Value)
            InfoLabel.Text = "TARGET: Player Stat\nName: " .. obj.Name .. "\nType: " .. obj.ClassName .. "\nValue: " .. tostring(obj.Value)
        else
            CommandBox.Text = obj.Name
            InfoLabel.Text = "TARGET: " .. typeTag .. "\nName: " .. obj.Name .. "\nPath: " .. obj:GetFullName()
        end

        if clickConn then clickConn:Disconnect() end
        clickConn = ExecuteBtn.MouseButton1Click:Connect(function()
            if typeTag == "DATA" then
                obj.Value = tonumber(CommandBox.Text) or CommandBox.Text
            elseif typeTag:find("GUI") then
                obj.Enabled = not obj.Enabled
            end
            ExecuteBtn.Text = "SUCCESS"
            task.wait(0.5); ExecuteBtn.Text = "EXECUTE / SET"
        end)
    end)
end

local function runOmniScan()
    -- Clear current list
    for _, v in pairs(Scroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    
    -- 1. GUIs & HIDDEN GUIs
    for _, g in pairs(PlayerGui:GetChildren()) do
        if g:IsA("ScreenGui") and g ~= ScreenGui then
            addEntry(g, g.Enabled and "GUI" or "HIDDEN GUI")
        end
    end
    
    -- 2. PLAYER DATA & STATS (Cash, Rebirths, Speed)
    for _, v in pairs(Player:GetDescendants()) do
        if v:IsA("ValueBase") then addEntry(v, "DATA") end
    end

    -- 3. COMMANDS, EVENTS & REMOTES (Server/Admin/Player)
    local function scanSystem(parent)
        for _, child in pairs(parent:GetChildren()) do
            if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
                addEntry(child, "CMD")
            elseif not child:IsA("Player") then
                pcall(function() scanSystem(child) end)
            end
        end
    end
    scanSystem(ReplicatedStorage)
    scanSystem(workspace)
end

-- 8. SEARCH & UPDATE
Search:GetPropertyChangedSignal("Text"):Connect(function()
    local f = Search.Text:lower()
    for _, b in pairs(Scroll:GetChildren()) do
        if b:IsA("TextButton") then b.Visible = b.Text:lower():find(f) ~= nil end
    end
end)

-- Start Scanning Loop
task.spawn(function()
    while true do
        runOmniScan()
        task.wait(2)
    end
end)
